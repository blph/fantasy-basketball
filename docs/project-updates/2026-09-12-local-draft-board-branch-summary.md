# `local-draft-board` — a local copy of the draft board for agents, verified against the live sheet

- Date: 2026-09-12 to 2026-09-13
- Branch: `local-draft-board` (27 commits, including this summary)
- Status: **built, deployed to the live sheet, and verified there**, including the tick scenario
  on a copy (see "Update, 2026-09-13" below). C1 is verified in the sheet.
- Constraint honoured: no player row, projection or ADP value appears below
  ([ADR-0006](../decisions/ADR-0006-no-provider-data-redistribution.md)).

---

## In one paragraph

The board now exists twice: the Google Sheet a person drafts from, and a local JSON snapshot
with a Python mirror of the sheet's draft-day formulas that an agent reads and ticks through
`board.py`. One `build_data.py` run writes both. They are kept in step by applying the same
change to both, never by pulling one from the other, and `verify.py --local` holds the engine
to the live sheet.

## Why

| Owner requirement | Met by |
|---|---|
| A format built for agents, not people | Compact JSON snapshot, read only through `board.py` |
| Never committed or pushed | Lives under gitignored `data/draft-board/`; `check-no-data.sh` blocks it anywhere; AGENTS.md boundary |
| The sheet's live calculations run locally | `board_engine.py` |
| Kept in sync by applying the same change to both | One build writes both; `verify.py --local` checks the live sheet; AGENTS.md change-both rule |
| Faster for an agent than Playwright | One local CLI call per question, compact output |
| Every dependent accounted for | Export, mock-draft guard, docs, CI, agents file |

## What was built

| File | What it does |
|---|---|
| `scripts/draft-board/board_settings.py` | The one source of league and draft-day settings |
| `scripts/draft-board/board_snapshot.py` | Snapshot paths, digest, compact dump, verified load |
| `scripts/draft-board/board_engine.py` | The sheet's draft-day formulas: rank, tiers, round, GAP, best build, strengths/weaknesses, Left @pos, tracker, punts |
| `scripts/draft-board/board_names.py`, `board_state.py`, `board.py` | The agent CLI with pinned draft state, a lock, undo and backups |
| `scripts/draft-board/pull_sheet.py` | Pulls the live sheet through gviz, sequentially, anchoring sparse ranges |
| `scripts/draft-board/verify_local.py` | Compares a pull against the engine (`verify.py --local`) |
| `scripts/draft-board/board_layout.json` | The sheet's layout, generated by the harness from synthetic data |
| `build_data.py`, `verify.py`, `export_yahoo_rankings.py`, `review_mock_draft.py`, `Build.gs`, `harness.js`, `check-no-data.sh` | Snapshot beside Data.gs, snapshot checks, `--local` export, fail-fast on a current-layout pull, settings defaults and data stamp, layout and stamp assertions, wider data guard |

Tests: 604 passed.

## Decisions

- Approach A (snapshot, stdlib engine, CLI, live parity check); SQLite views and pulling the
  sheet to CSV rejected ([ADR-0022](../decisions/ADR-0022-local-draft-board.md)).
- Win-rate cutoffs 40/60/75 on both boards. A category sitting at the ~60% target now reads
  STRONG ([ADR-0023](../decisions/ADR-0023-win-rate-cutoffs-40-60.md), superseding ADR-0018 in part).
- Mock-draft replay deferred with the `review_mock_draft.py` port.
- Declined review findings: see the design spec §7.

## Live-sheet defects found

- **C1** — a reorder refresh blanked unoverridden My GP. Fixed in `Build.gs`, deployed, and
  verified in the sheet by the tick scenario on 2026-09-13.
- **I1** — after a reorder refresh, GONE/MINE ticks can land beside the wrong players. Written
  up in [the bug report](../bugs/2026-09-12-reorder-refresh-tick-misattachment.md); fix deferred.
- "Data generated" went stale after Refresh — fixed by the date and digest stamp.
- The SORT STALE claim and the cheat sheet's GONE-benchmark claim were false — corrected.
- Tracker raw totals are always HBP's line — documented.
- Found during deployment, in `pull_sheet.py` (fixed): gviz drops empty rows anywhere in a
  range, not only at the end, which shifted cells; a single parallel fetch failed live; blank
  never-ticked checkboxes were refused; a column named `BY` collided with a gviz keyword.
- Found during deployment, in the sheet: a Full rebuild did not carry MINE ticks across. They
  were restored by name.

## Deployment record

- Deployed `Build.gs` from commit `aac328f`; `Build.gs` is unchanged since.
- `Code.gs` 2,598 → 2,653 lines; `Data.gs` pushed with digest prefix `d3fc20946679`,
  projections dated 2026-09-12. Both verified before saving and after a reload.
- Before the rebuild: 7 MINE and 0 GONE ticks recorded by name; no My GP overrides, XRank,
  GP history or notes; no Settings input differed from the defaults.
- Menu actions, in order: Full rebuild, Refresh data (same players, same order), Rebuild &
  re-sort. After each, 7 MINE and 0 GONE.
- Browser artefacts are in the gitignored `.playwright-cli/` and `data/draft-board/`.

### Verified in the sheet

- Settings sanity: both alignment rows read `aligned`; the stamped digest equals the snapshot's.
- `verify.py --local` against the live sheet: exit 0. 15,596 Draft Board cells on 200 rows,
  8 tracker rows and a roster of 7, 9 punt blocks, 10 Settings inputs.
- Tie order: the sheet breaks ties the engine's way (Draft Board 4 of 4 tied rows, Punts 7 of 7).
- Screenshots of the Draft Board and Category Tracker: no error cells, sort label current,
  tracker shows 7 players ticked.
- `board.py` practice run on the real snapshot: pinned to 2026-09-12, a pick and its undo
  recorded correctly, `check` exit 0; practice state removed.
- **Tick scenario on a copy: not completed.** The copy was made and authorised, and its ticks
  cleared (verify exit 0), but clearing removed the copy's checkboxes, so the pick, sort, swap
  and override steps could not run. The copy was deleted and the deletion confirmed.

### Known data issue

One BMP ZSC value differs from Basketball Monster's published column by 0.0263 (tolerance
0.02; average error 0.003). It predates this branch — `main` builds identical numbers — and was
already live. Deployed by the owner's decision. The likely cause is a projection revised after
the 2026-09-12 export.

## Known gaps

Conditional-format bindings and colours; the disagreement gap baked into rules at build time;
I1 (ticks read back from the sheet are taken as truth); a My GP override indistinguishable from
its seed formula in a values pull.

## What remains

### Blocking, before the board is used for a real draft
1. `review_mock_draft.py` port — still blocking (see [the 2026-09-01 summary](2026-09-01-draft-refactoring-branch-summary.md)). It now fails fast
   instead of misreading.
2. I1 — the reorder-refresh tick misattachment.

### Worth doing, not blocking
4. Re-export and recalibrate to clear the BMP ZSC outlier.
5. Full rebuild should carry MINE ticks across.
6. The Chrome draft assistant and pick recommendations, built on `board_engine.py`.
7. Mock-draft replay, with the port.
8. Tighten `verify_local.py`'s tie-group set comparison to a positional one — the live tie
   order matched.

## Update, 2026-09-13: fast pull and a scripted tick scenario

- **`pull_sheet.py` downloads the workbook as one xlsx** through the owner's browser instead
  of ~94 gviz requests: about 3 seconds instead of 1.5 minutes. xlsx stores numbers to 10
  significant digits, so a pull records `sig_digits: 10` and `verify.py --local` compares at
  that precision; full-precision pulls still compare to 1e-9. The last gviz pull and an xlsx
  pull of the live sheet agreed on every cell the ticks do not drive.
- **`tick_scenario.py` runs the whole tick scenario as one command** on a throwaway copy:
  inherited ticks cleared, MINE 8 then 14, GONE on 10, one then two punted categories, a new
  sort with Rebuild & re-sort (the copy's authorisation, allowed with the owner's approval),
  and a reordering Refresh with a hand value and two swapped names on clean rows. Every step
  passed `verify.py --local` in full (exit 0), the hand value stayed on its player, and the copy
  was deleted permanently and confirmed gone. That pass closes C1 in the sheet. One run takes
  about 8.5 minutes of wall-clock and a single line per step of output.
- Found on the live sheet: the Board's `My GP` (W) and `XRank` (Z) columns are hidden, so the
  Name Box cannot reach them; the scenario's hand value goes in `GP Y-1`. The live sheet also
  showed 0 MINE ticks where the 03:33 pull had 7; the owner chose to leave them unticked.
- Six earlier attempts each left a test copy behind; every one was found by id, confirmed by
  its contents, and permanently deleted. The older, unstamped copy in the owner's Drive was not
  touched.

## Records

New: ADR-0022 · ADR-0023. Superseded in part: ADR-0018. New bug report:
`docs/bugs/2026-09-12-reorder-refresh-tick-misattachment.md`.
